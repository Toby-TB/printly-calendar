# 🗓️ 拾光行事历 · 月历打印工坊

一个月视图行事历 Web 应用：以「星期为列、日期为网格」，支持 DIY 风格与预设模板，
并针对**打印 + 打孔装订成册**做了专门设计。

> English documentation: [README_EN.md](./README_EN.md)

## 快速开始

**最简单的方式（无需下载、无需安装）**：直接打开在线地址

👉 **https://toby-tb.github.io/printly-calendar/**

任何能上网的人（手机、平板、电脑浏览器）打开上面的网址就能用。

**想离线使用 / 存一份到自己电脑**：

1. 在仓库主页点绿色 `Code` 按钮 → `Download ZIP`，解压后双击 `index.html`；
   或直接下载单文件版 `calendar-all-in-one.html`，双击打开即可
2. 也可以本地起服务（可选）：
   ```bash
   python3 -m http.server 8080
   # 浏览器访问 http://localhost:8080
   ```

日程数据保存在浏览器 `localStorage`，可导出 / 导入 JSON 迁移。

## 新手使用步骤

1. **看月历**：中间就是当月日历，今天是高亮圈，彩色小条是日程
2. **加日程**：点击任意日期 → 填标题、日期、时间、分类 → 点「保存」
3. **改日程**：点击日历上的彩色小条，或左侧「本月日程」列表里的条目
4. **换风格**：左侧「🎨 设计」→ 挑一个预设模板，再按喜好调主色 / 纹理 / 字体
5. **打印成册**：左侧「🖨 打印」→ 选起始月和结束月 → 「生成打印预览」→ 检查后点「打印」
   - 每页 A4，顶部自动留 26mm 装订区，可选打孔标记，适合打圆孔 / 装订圈合成册

## 语言切换

右上角下拉菜单支持三种语言，切换即时生效，界面、日历、打印页全部同步翻译：

- 简体中文
- 繁體中文
- English

语言偏好会自动保存，下次打开沿用上次选择；首次打开会尝试跟随浏览器语言。

## 功能清单

### 📋 日程管理
- 月视图：7 列星期 × 6 行日期网格，可切换「周一 / 周日」起始
- 今天高亮、周末底纹、相邻月份日期淡化
- 点击日期添加日程；点击色条编辑 / 删除
- 支持：标题、开始日期、结束日期（跨天）、时间、分类颜色、备注
- 分类图例：内置 6 类，可自定义新增 / 删除，点击图例可隐藏该分类
- 侧栏「本月日程」快速浏览与编辑

### 🎨 DIY 设计
- 8 套预设模板：奶油手账 / 抹茶清新 / 莫兰迪 / 复古牛皮纸 / 樱花粉 / 午夜蓝 / 极简白 / 海盐蓝
- 自定义：主色、纸张背景、纹理（波点 / 方格 / 横线 / 斜纹 / 星点）、字体、圆角、背景图片
- 元素开关：打印装订孔标记、装饰角标、网格线、周末底纹
- 所有设置即时生效并自动保存

### 🖨 打印装订
- **选择日期范围**：起始月 + 结束月，快速选「本年度」「未来 12 个月」
- 范围中的每个月生成一页 A4（可预览逐页检查）
- **每页顶部自动预留 26mm 装订区**，并有虚线引导 + 可选两个打孔标记（圆钉 / 装订圈位置）
- 可选首页封面（封面标题、范围月份高亮）
- 底部备注区、页码
- 打印页使用当前 DIY 主题（颜色 / 纹理 / 背景图），建议打印时开启浏览器「背景图形」
- 支持跨天日程在打印页连续标注

## 文件结构

```
├── index.html             # 页面结构
├── styles.css             # 界面 + 打印样式（@page A4）
├── app.js                 # 全部逻辑 + 三语翻译（无依赖）
├── calendar-all-in-one.html  # 单文件版（CSS/JS 已内联，双击即用）
├── calendar-app.zip       # 打包下载
├── README.md              # 中文说明
└── README_EN.md           # English documentation
```

## 使用提示

- 打印前先点「打印预览」核对页数；Chrome 打印设置中选 `A4 / 纵向 / 边距：无 / 背景图形：开启`
- 打孔位置默认两个圆孔，圆心距左右各 65mm（A4 标准两孔装订位），顶部 10.5mm
- 封面在「打印」面板中可关闭；封面标题可自定义
- 模板切换会重置自定义颜色 / 纹理（背景图也会移除），先选模板再微调更省事
